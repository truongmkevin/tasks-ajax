$(document).ready(function(){
  $('#user_task_due').datepicker({
    format: "yyyy-mm-dd",
    todayBtn: true,
    autoclose: true,
    orientation: "top auto",
    todayHighlight: true,
    toggleActive: true
  });
});